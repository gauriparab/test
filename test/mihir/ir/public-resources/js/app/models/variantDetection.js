/*global define:false*/
define(['jquery', 'underscore', 'models/baseModel'],
    function($, _, BaseModel) {
        "use strict";
      
    var VariantDetection = BaseModel.extend({
               
    });

    return VariantDetection;

});
