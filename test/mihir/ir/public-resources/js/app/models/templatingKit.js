/*global define:false*/
define(['jquery', 'underscore', 'models/baseModel'],
    function($, _, BaseModel) {
        "use strict";

        var TemplatingKit = BaseModel.extend({
        });

        return TemplatingKit;

    });
