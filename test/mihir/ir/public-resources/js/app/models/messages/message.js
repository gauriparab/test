/*global define:false*/
define(['backbone'], function (Backbone) {

    "use strict";

    var Message = Backbone.Model.extend({
        // empty model populated by collection...
    });

    return Message;
});
