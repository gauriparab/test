/*global define:false*/
define(['underscore', 'models/baseModel'], function(_, BaseModel) {

    'use strict';
    
    var FilterigMetrics = BaseModel.extend({
    	
    });

    return FilterigMetrics;
    
});
