/*global define:false*/
define(['underscore', 'models/baseModel'], function(_, BaseModel) {

    'use strict';
    
    var AlleleCoverage = BaseModel.extend({
    	
    });

    return AlleleCoverage;
    
});
