/*global define:false*/
define(['underscore', 'models/baseModel'], function(_, BaseModel) {

    'use strict';
    
    var HistoGram = BaseModel.extend({
    	
    });

    return HistoGram;
    
});
